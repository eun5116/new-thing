# AGENTS.md

## 프로젝트 문서 작성 규칙

- 이 저장소의 설명 문서, 실험 보고서, 분석 메모, 진행 기록은 기본적으로 한국어로 작성한다.
- 코드 식별자, 파일명, 설정 키, 컬럼명, 명령어, 로그 원문은 영어 원문을 유지한다.
- 보고서는 `reports/` 아래에 저장하고, 파일명은 기존처럼 영어 snake_case를 사용해도 된다.
- 보고서 본문은 한국어로 쓰되, 수치와 실험 ID는 정확히 유지한다.
- 새 실험을 수행하면 최소한 아래 내용을 보고서에 포함한다.
  - 실험 목적
  - 사용 데이터와 기간
  - 주요 설정
  - 검증 결과
  - 테스트 결과
  - 해석
  - 다음 작업

## 프로젝트 방향

이 프로젝트는 한국 주식 RL 매매 실험을 위한 데이터/feature/평가 체계를 구축한다.

현재 우선순위는 다음과 같다.

1. KRX OpenAPI 기반 공식 데이터를 사용한다.
2. 학습/분석용 데이터는 Parquet 중심으로 유지한다.
3. Buy & Hold뿐 아니라 MA20/60 같은 강한 기준선을 항상 비교한다.
4. 단순 수익률보다 drawdown, Sharpe, 검증/테스트 일관성을 함께 본다.
5. PPO가 처음부터 모든 매매 규칙을 배우게 하기보다, 강한 기준선을 바탕으로 언제 벗어날지 학습하는 방향을 검토한다.

## 주의 사항

- 미래 정보가 feature에 들어가지 않도록 한다.
- 외부 사건 feature는 `effective_date` 기준으로 모델이 관측 가능한 날짜에만 반영한다.
- 사용자가 명시적으로 요청하지 않는 한 기존 산출물과 모델을 삭제하지 않는다.
- 큰 변경 후에는 `PYTHONPATH=src .venv/bin/python -m pytest -q`를 실행해 확인한다.
