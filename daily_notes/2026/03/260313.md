- 무엇을: two_week_momentum_watch 스펙을 weekly report에 연결하고, 알림 유니버스를 고정 상위 종목이 아니라 S&P 500 전체와 KOSPI 전체로 넓혔다.
- 왜: 기존 구현은 5거래일 변화율 리포트만 제공했고, 알림도 일부 종목만 대상으로 해 사용자가 원한 "시장 전체에서 조건 충족 종목 탐지"와 달랐다.
- 어떻게: dataset SoT를 읽는 경량 YAML 파서를 추가하고, `market_scope`가 `sp500`, `kospi`인 config를 해석하도록 바꿨다. 10거래일 tolerant 판정 함수를 구현해 메일 본문에 시장별 트리거 종목 섹션을 넣었고, 회귀 방지를 위해 알림 판정과 SoT config/유니버스 해석 테스트를 추가했다.
- 결과: 알림 로직과 리포트 출력이 연결되었고, S&P 500 전체와 KOSPI 전체를 스캔하는 설정이 준비되었다. `python -m unittest tests/test_two_week_momentum_watch.py`, `python -m py_compile ...` 검증을 통과했다.
- 변경 파일: daily_notes/2026/03/260313.md, configs/dataset/us_equities_alerts.yaml, configs/dataset/kospi_alerts.yaml, weekly_market_report/weekly_report.py, tests/test_two_week_momentum_watch.py
- 다음: 운영 환경에서 실제 시장 데이터를 대상으로 한 번 실행해 응답 시간과 누락 종목 비율을 점검하고, 필요하면 배치 크기와 캐시 전략을 보강한다.
